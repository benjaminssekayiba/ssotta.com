# Ssotta Remotion Explainer Film

This folder contains the Remotion source for the homepage explainer film.

Run:

```bash
npm run video:studio
npm run video:render
```

`video:render` writes `public/ssotta-explainer.mp4`. The website homepage video panel will use that MP4 automatically when it exists, and otherwise shows a polished animated fallback.
