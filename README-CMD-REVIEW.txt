Ssotta npm preview

Open CMD in this folder, then run:

  npm install
  npm run dev

Then open:

  http://localhost:3001

This folder is a standalone preview copy made for npm. The original Replit export uses pnpm workspaces.
